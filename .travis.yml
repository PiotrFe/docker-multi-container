sudo: required
services:
  - docker

before_install:
  - docker build -t piotrfer/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true piotrfer/react-test npm test -- --coverage

after_success:
  - docker build -t piotrferensztajn/multi-client ./client
  - docker build -t piotrferensztajn/multi-nginx ./nginx
  - docker build -t piotrferensztajn/multi-server ./server
  - docker build -t piotrferensztajn/multi-worker ./worker

  # Log in to the Docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take images and push them to docker hub
  - docker push piotrferensztajn/multi-client
  - docker push piotrferensztajn/multi-nginx
  - docker push piotrferensztajn/multi-server
  - docker push piotrferensztajn/multi-worker
